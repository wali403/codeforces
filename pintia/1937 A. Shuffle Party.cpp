#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

	int t, tmp;
	cin >> t;
	for(unsigned i = 0; i < t; ++i) {
		cin >> tmp;
		vector<int> v(tmp);
		for(unsigned j = 1; j <= tmp; ++j) {
			v[j] = tmp;
		}

		for(unsigned j = 0; j < tmp; ++i) {
			int lg = v[j] /
		}
	}

	return 0;
}
