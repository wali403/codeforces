#include <bits/stdc++.h>
#include <vector>
using namespace std;

//要求两类人群的规模尽可能接近，而他们的总活跃度差距尽可能拉开。

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

	int n;
	cin >> n;

	vector<int> v(n);

	for(unsigned i = 0; i < n; ++i) {
		cin >> v[i];
	}

	return 0;
}
