#include <bits/stdc++.h> 
#include <vector>
using namespace std;

#define ll long long 
#define mod (100000)

int dp[mod][mod] {};

int main() {    
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 

	ll t;
	cin >> t;

	ll n, k;
	for(unsigned i = 0; i < t; ++i) {
		cin >> n  >> k;
		vector<ll> p(n);
		for(unsigned j = 0; j < n; ++j) {
			/* code */
		}
	}

	return 0;
}