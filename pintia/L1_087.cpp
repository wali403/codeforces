#include <bits/stdc++.h> 
  
int main() {
    using namespace std;

    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 

	int n, m, q, t, c;
	cin >> n >> m >> q;

	for (int i = 0; i < q; i++) {
		cin >> t >> c;

		if (t == 0) {
			//row
		} else {
			//column
		}
	}

	return 0;
}