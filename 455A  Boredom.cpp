#include <iostream>
#include <vector>
int main(int argc, char const *argv[]) {
	
	using namespace std;

	int num;
	cin >> num;

	std::vector<int> v;
	for (int i = 0; i < num; ++i) {
		int tmp;
		cin >> tmp;
		v.push_back(tmp);
	}

	for (int i = 0; i < num; ++i) {
		
	}

	return 0;
}