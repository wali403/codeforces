#include <iostream>
#include <vector>

int main(int argc, char const *argv[]) {
	
	using namespace std;

	int n;
	cin >> n;

	vector<int> v;
	for (int i = 0; i < n; ++i)	{
		int t;
		cin >> t;
		v.push_back(t);
	}

	

	return 0;
}