#include <bits/stdc++.h> 
#include <vector>
using namespace std;

const int mod = 2e5+10;
typedef long long ll;
#define x first
#define y second
typedef pair<int, int> P;

vector<int> e[mod] {};
char w[mod] {};
int main() {    
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 
    cout.tie(NULL); 

    int n,m,x,y;
    char c;
    for (int i = 1; i <= m; i++) {
    	cin >> x >> y >> c;
    	cin.ignore(3);

    	if (c == L)
    }


	return 0;
}