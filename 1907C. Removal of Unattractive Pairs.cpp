#include <bits/stdc++.h> 
using namespace std;

void f() {
	int n;
	cin >> n;
	string s;
	cin >> s;

	for (int i = 0; i < s.size(); i++);
}

int main() {    
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 
    cout.tie(NULL); 

    int t;
    cin >> t;
    while (t--) f();

	return 0;

}
