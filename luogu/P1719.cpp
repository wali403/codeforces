#include <bits/stdc++.h> 
using namespace std;

#define mod (100001)
#define ll long long

int main() {    
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 

    int n;
    cin >> n;

    vector<vector<int>> v(n, vector(n));

	return 0;
}